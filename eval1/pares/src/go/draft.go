package main
import "fmt"
func main() {
    var A, B int
    fmt.Scan(&A,&B)

    if A > B {
        fmt.Println("invalido")
    } else {
        for ;A <= B; A++ {
            if A % 2 == 0 {
                soma +=
            }
        }

    }
}
